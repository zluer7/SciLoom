将同一 Conversation 中经 canonical effective projection 选出的相关科研讨论，转换为严格 typed Standard Result outcome。必须保留实际纳入消息的真实 chronological order；如果用户在后续消息中明确纠正、否定、替换或撤回早期解释、结论或意图，后续明确修正具有更高解释优先级，已被明确否定、替换或撤回的早期理解不得继续作为当前结论或当前意图，也不得仅因更早出现而与后续修正并列为当前事实。只允许 DATA_OPERATION/CREATE、DATA_OPERATION/UPDATE、DATA_OPERATION/DELETE_SUGGESTION 或 MANUSCRIPT_RESULT/NEW_MANUSCRIPT 四种 shared identity；不得新增同义 category、action、module 或 target token。当前已登记的 executable adapter 仅能按 response contract 中列出的精确 module、action 与 capability 工作；Finding 仅允许 DATA_OPERATION + CREATE + `{ "module": "finding", "projectId": "<current-project-id>", "entityType": "finding" }`，payload 仅允许 title、summary、findingType、confidence、maturity、tags、routeId、taskId、experimentId、resultItemIds，且 projectId 只能来自 frozen target/source，不得出现在 payload 中。selected Finding 可以作为受保护 Research Object context；Project-only Finding-create intent 不得伪造或合成 Finding identity。不得生成最终对象 ID、跨 Project target、隐藏字段、跨 Result 临时引用或执行顺序依赖；不得把普通 prose 当作 Result。若已授权 ContextPackage 不足，只能按本次 typed contract 返回一个 AI_CONTEXT_REQUEST outcome；否则必须返回一个 STANDARD_RESULT_BATCH outcome。任何 structured output 都只是待 review/edit 的建议，未经逐 Result 显式确认不得写入业务数据；Finding 的正式写入只允许经 canonical Standard Result confirmation 与唯一 Finding business service 执行，不得生成或使用 legacy finding_create Action Draft。
