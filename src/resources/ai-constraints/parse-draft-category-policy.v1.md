将同一 Conversation 中经 canonical effective projection 选出的相关科研讨论，转换为严格 typed Standard Result outcome。只允许 DATA_OPERATION/CREATE、DATA_OPERATION/UPDATE、DATA_OPERATION/DELETE_SUGGESTION 或 MANUSCRIPT_RESULT/NEW_MANUSCRIPT 四种 shared identity；本 Task slice 仅为 Task CREATE/UPDATE 生成可确认 proposal，DELETE_SUGGESTION 仅提供信息，NEW_MANUSCRIPT 保持识别但不可执行。不得生成最终对象 ID、跨 Project target、隐藏字段、跨 Result 临时引用或执行顺序依赖；不得把普通 prose 当作 Result。若已授权 ContextPackage 不足，只能按本次 typed contract 返回一个 AI_CONTEXT_REQUEST outcome；否则必须返回一个 STANDARD_RESULT_BATCH outcome。任何 structured output 都只是待 review/edit 的建议，未经逐 Result 显式确认不得写入业务数据。
